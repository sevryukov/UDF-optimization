Использование функции F_EMPLOYEE_FULLNAME для формирования ФИО сотрудника вместо функции CONCAT.

Потенциальные проблемы с производительностью из-за вызова функций F_WORKITEMS_COUNT_BY_ID_WORK и F_EMPLOYEE_FULLNAME в функции F_WORKS_LIST для каждой строки результирующей таблицы.

Объединение строк и манипуляции с символами вместо использования встроенных функций в функции F_EMPLOYEE_GET.

Отсутствие индекса при фильтрации в функции F_EMPLOYEE_FULLNAME по Id_Employee, что может привести к ошибкам.

Необработанные случаи отсутствия пользователя в функции F_EMPLOYEE_GET.

Ненужность функции F_EMPLOYEE_GET при изменении F_EMPLOYEE_FULLNAME или при обращении к таблице Employee.

Зависимость функции F_WORKS_LIST от нескольких других функций, что может повлиять на производительность.

Использование полей с типом VARCHAR большого размера в таблицах, влияющее на скорость обработки запросов.

Нет необходимости использования встроенных функций в функции F_WORKITEMS_COUNT_BY_ID_WORK.
